{
  "name": "health3",
  "type": "module",
  "scripts": {
    "start": "echo 'no start script'",
    "prebuild": "echo 'no prebuild step required'",
    "build": "echo 'no build step defined'",
    "pretest": "echo 'no pretest step required'",
    "test": "echo 'no tests defined'"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  }
}
